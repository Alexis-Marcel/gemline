/bin/
/dist/
*.exe
*.test
*.out
coverage.txt
.DS_Store
.idea/
.vscode/
node_modules/
web/dist/
.screenshots/
.env
.env.local
.env.*.local

# Local Terraform state — contains cluster secrets, never commit.
*.tfstate
*.tfstate.*
.terraform/

# Backup of the Sealed Secrets master key. Stored in a password manager
# or offline drive; if a copy lands in the working tree by accident
# this rule keeps it out of git.
sealed-secrets-master-key*.yaml
