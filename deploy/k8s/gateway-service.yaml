apiVersion: v1
kind: Service
metadata:
  name: gemline-gateway
  namespace: gemline
  labels:
    # servicemonitor.yaml selects on this label — keep them in sync.
    app: gemline-gateway
spec:
  type: ClusterIP
  selector:
    app: gemline-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
